#include <stdio.h>
int main() {
  int i,n,count;
  printf("Nhap n: ");
  scanf("%d",&n);
  int s[n];
  for (i=0;i<n;i++) {
    printf("s[%d]=",i);scanf("%d",&s[i]);
  }
  count=0;
  for (i=0;i<n;i++) {
    if (s[i]==0)
      count++;
  }
  printf("0 appear %d times\n",count);
  return 0;
}

