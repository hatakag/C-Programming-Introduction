#include <stdio.h>
main() {
       int sum;		/* Variable declaration */
       	   		/* sum is a variable hold the sum of two interger */
       sum = 75 + 25;	/* Value assignment */
       printf("The sum of 75 and 25 is %d/n", sum);
}